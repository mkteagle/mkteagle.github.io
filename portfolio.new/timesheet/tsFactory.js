(function () {
    'use strict';
    angular.module("tsFactory", ["ngResource"])
        .factory("tsFactory", ['$resource', function($resource) {
            var resource = $resource("")

        }]);
})();